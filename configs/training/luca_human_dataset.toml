# Dataset Configuration for Luca Human LoRA
# Only dataset-related settings (for --dataset_config)
# Training parameters MUST be passed via CLI

[[datasets]]
resolution = 512
batch_size = 8
enable_bucket = true
min_bucket_reso = 384
max_bucket_reso = 768
bucket_reso_steps = 64
bucket_no_upscale = false

  [[datasets.subsets]]
  image_dir = "/mnt/data/ai_data/datasets/3d-anime/luca/curated_dataset_v2_smart/luca_human/images"
  num_repeats = 1
  shuffle_caption = true
  keep_tokens = 3
  caption_extension = ".txt"
  color_aug = false
  flip_aug = false
